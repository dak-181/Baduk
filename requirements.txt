# Core dependencies
pygame>=2.6.0
tensorflow>=2.16.0  # includes Keras and GPU support — no separate keras or tensorflow-gpu needed
numpy>=1.26.0

# GPU acceleration (recommended)
# CUDA 12.3+ and cuDNN must be installed at the system level separately.
# TensorFlow 2.16+ will automatically use the GPU if CUDA is present.